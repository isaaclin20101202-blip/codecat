#include <bits/stdc++.h>
using namespace std;

int main()
{
    int ans = 0;
    for (int i = 0; i < 10; i++)
        {
            int temp;
            cin >> temp;
            ans+=temp;
        }
    cout << ans;
}

